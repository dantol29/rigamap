import { createNativeStackNavigator } from '@react-navigation/native-stack';
import MapView, { Marker, Polygon, Callout } from "react-native-maps";
import { StyleSheet, Text, View , KeyboardAvoidingView,ScrollView, 
  Platform, StatusBar, SafeAreaView, Image, ImageBackground, Dimensions} from 'react-native';
import { useRef, useState, Label, Item, Body } from 'react';
import { Button, CheckBox, Input, ListItem } from 'react-native-elements';
import { NavigationContainer } from '@react-navigation/native';
import { createDrawerNavigator } from '@react-navigation/drawer';
import { createMaterialBottomTabNavigator } from '@react-navigation/material-bottom-tabs';
import { MaterialCommunityIcons } from '@expo/vector-icons';
import Icon from 'react-native-vector-icons/AntDesign';
import { TouchableOpacity } from 'react-native-gesture-handler';
import Sidebar from './customDrawer';

function Home(){


            
const Stack = createNativeStackNavigator();

}

const styles = StyleSheet.create({
    
})
export default Home;